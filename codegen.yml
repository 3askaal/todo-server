overwrite: true
schema: "http://localhost:4000/graphql"
documents: null
generates:
  ../shared/types/todo.d.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
      - "typescript-mongodb"
    config:
      typesPrefix: T
  ./graphql.schema.json:
    plugins:
      - "introspection"
